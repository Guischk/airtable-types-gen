#!/usr/bin/env node

// Require the CLI directly rather than spawning a second `node`. The spawn
// wrapper doubled process startup and, because `child.on('exit')` reports null
// for signal-terminated children, `process.exit(null)` turned a Ctrl-C into a
// success exit code.
require('../dist/cli/index.js');
